{
  "build": {
    "env": {
      "PLAYWRIGHT_BROWSERS_PATH": "/root/.cache/ms-playwright"
    },
    "postInstall": "playwright install chromium && playwright install-deps"
  },
  "start": "python3 ihouse_checker_playwright.py"
}
